/** The public product name shown in metadata and authentication copy. */
export const siteName = 'RedactPDF.ai';

/** Canonical origin used for Open Graph URLs and metadataBase. */
export const siteUrl = 'https://redactpdf.ai';

/**
 * Search-result and social description. Leads with “redact a PDF free”
 * because that is the query family the landing page targets.
 */
export const siteDescription =
  'Redact a PDF free in seconds. Upload, review AI suggestions, and download a permanently redacted file. Open source — files deleted within one hour.';

/** Document title and Open Graph title for the marketing homepage. */
export const siteTitle = 'Free Automatic PDF Redaction | RedactPDF.ai';

/** Public GitHub repository linked from “view the source”. */
export const githubRepoUrl = 'https://github.com/opengreencard/redactpdf';

/** Companion immigration product this tool was built to support. */
export const openGreenCardUrl = 'https://opengreencard.com';

/** Travel planner from the same team, used as a trust signal on the landing page. */
export const wanderlogUrl = 'https://wanderlog.com';
